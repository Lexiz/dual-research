# Handover -- SPEC-0057 (Timeline + critique restructure)

**Date:** 2026-05-17
**Spec:** [0057](../specs/0057-timeline-critique.md)
**PR:** [#61](https://github.com/Lexiz/dual-research/pull/61)
**Version:** 0.54.0 -> 0.55.0
**Status:** `succeeded`
**Deploy:** `dual-research-alex.fly.dev/api/health` -> `{"ok":true,"version":"0.55.0","backend":"supabase"}`

---

## What shipped

Timeline + critique pane restructure delivering all four SUR-09..11, SUR-14 surface changes plus the deferred CardHeadline Chip migration from SPEC-0052 D10. Sticky PhaseRail added to the timeline pane left edge showing phase progress with click-to-scroll. ArtifactCard migrated from inline-styled divs to the Card/CardBody primitives. ChipCluster primitive added to shared.jsx wrapping StatsChips with max-5 overflow. Critique pane gained two new filter axes (agent + status) alongside the existing kind filter, all combining with AND logic. DriftCluster group separates ghosted items above Open/Resolved. Summary tab now opens with the highest-leverage open thread (most ghosted rounds) rendered as a QuestionThread. CardHeadline kind/status badges migrated to Chip primitives via toneFromColor mapper. PaneButton/PaneButtonGroup dead code removed.

### Changelog IDs implemented

`SUR-09` (PhaseRail + Card migration), `SUR-10` (ChipCluster), `SUR-11` (3-axis filter + DriftCluster), `SUR-14` (Summary highest-leverage thread).

### Files touched

- `src/dual_research/ui/static/run-detail.jsx` -- PhaseRail component, ArtifactCard -> Card/CardBody migration, 3-axis critique filter (agent+status), DriftCluster grouping, Summary highest-leverage thread, CardHeadline -> Chip migration, _toneFromColor helper, PaneButton/PaneButtonGroup removed
- `src/dual_research/ui/static/shared.jsx` -- ChipCluster component added, exported to window
- `src/dual_research/ui/static/components.css` -- .cc (ChipCluster) + .phase-rail sections appended
- `src/dual_research/ui/static/index.html` -- cache-bust ?v=0057
- `src/dual_research/ui/static/how-it-works.jsx` -- VERSION_NOTES entry for 0.55.0
- `pyproject.toml` + `__init__.py` -- 0.54.0 -> 0.55.0
- `CHANGELOG.md` -- 0.55.0 entry
- `specs/0057-timeline-critique.md` -- spec doc
- `uv.lock` -- version sync

### Mid-flight refinements

- **D2 (Card migration)** -- draft proposed Card.expandable as a distinct variant. The existing Card primitive from SPEC-0052 already has `expanded` prop + `card-expanded` class + interactive hover. Used it directly with `<Card interactive expanded={expanded}>` + `<CardBody>` rather than creating a new variant. Flash-highlight ring moved to a wrapper div (Card primitive doesn't accept inline style for boxShadow).
- **D3 (ChipCluster)** -- draft proposed a `THIS TURN` label when >5. Dropped the label as unnecessary -- the +N overflow button is self-explanatory and less noisy.
- **D8 (PaneButton removal)** -- draft mentioned removing from shared.jsx. PaneButton was actually in run-detail.jsx (line 108-170). Removed from there. Confirmed zero call sites via grep.

---

## Risks the next spec should know about

- **Component-level invariants preserved:** 5-state ReconcileChip untouched (stays in header row 1). PhaseDots unchanged. BlockingItemCallout unchanged.
- **New patterns introduced:** `PhaseRail` component using `.phase-rail` CSS class + `data-phase-id` attribute on PhaseDivider for scroll targeting. `ChipCluster` in shared.jsx with `.cc` / `.cc-overflow` CSS. `_toneFromColor()` helper maps COLORS.* to Chip tone strings. Three-state filter (agentFilter + statusFilter) in CritiqueExplorer.
- **DriftCluster detection** uses `findLedgerGhostRounds()` which reads `run.phaseLedgers[phase]` -- same read-only pattern as BlockingItemCallout.
- **Dead code still present:** ChromeTab in app.jsx, FilterChip in errors.jsx. Carry-over from prior handovers.

---

## Downstream invalidations (if any)

- **SPEC-0058 draft may assume PaneButton exists.** It was removed in this spec. If SPEC-0058 references PaneButton, use Tab/TabGroup instead.
- Otherwise downstream drafts hold.

---

## Verification snapshot

- `uv run pytest tests/ -q` -> 725 passed.
- Preview-verify: dev server confirmed partner-vetting (3a4a) loads with PhaseRail, Card primitives, 3-axis filter, DriftCluster, and Summary highest-leverage thread.
- Both themes: CSS uses token variables throughout; no hardcoded colors in new code.
- Reduced-motion contract: no new animations added.
- Cache-bust: `?v=0057` in index.html (17 references).
- `/api/health`: `0.55.0` confirmed.

---

## For the orchestrator

- Next spec in queue: SPEC-0058.
- This spec's commit on main: `c7065a1`.
- Branch deleted: local + remote.
