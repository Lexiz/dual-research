# Handover -- SPEC-0055 (Run list -- sort + attention + filters + search)

**Date:** 2026-05-17
**Spec:** [0055](../specs/0055-run-list.md)
**PR:** [#59](https://github.com/Lexiz/dual-research/pull/59)
**Version:** 0.52.0 -> 0.53.0
**Status:** `succeeded`
**Deploy:** `dual-research-alex.fly.dev/api/health` -> `{"ok":true,"version":"0.53.0","backend":"supabase"}`

---

## What shipped

Run list rework delivering all four SUR-01..04 surface changes. Sortable columns with URL-persisted state (sort + filter + search as query params via `history.replaceState`). Attention-first section promotes errored and deadlocked runs to the top with a "Needs attention (N)" header and inline summary (phase, rounds, status). Errored rows get a 2px red left border, deadlocked get amber. Search input in the header bar with `/` keybinding for focus. Filter tabs wrapped in `<TabGroup>`. SUR-01 (PHASE column overlap) was verified already fixed by SPEC-0053's CMP-11 table header distinction -- no additional fix needed.

### Changelog IDs implemented

`SUR-01` (verified fixed), `SUR-02` (sortable + URL state), `SUR-03` (attention borders), `SUR-04` (attention-first section).

### Files touched

- `src/dual_research/ui/static/run-list.jsx` -- complete rewrite: sortable columns, URL state helpers, search input, `/` keybinding, attention-first section, errored/deadlocked left borders, TabGroup wrapper
- `src/dual_research/ui/static/index.html` -- cache-bust ?v=0055
- `src/dual_research/ui/static/how-it-works.jsx` -- VERSION_NOTES entry for 0.53.0
- `pyproject.toml` + `__init__.py` -- 0.52.0 -> 0.53.0
- `CHANGELOG.md` -- 0.53.0 entry
- `specs/0055-run-list.md` -- spec doc
- `uv.lock` -- version sync

### Mid-flight refinements

- **D1 (SUR-01 PHASE overlap)** -- the draft expected a stacking-context bug to fix. Inspection on current main showed SPEC-0053's CMP-11 work already resolved the header styling. Verified and documented; no code change needed.
- **D5/D6 (attention borders + section)** -- implemented as inline styles on RunRow rather than adding new CSS classes to components.css. The left border uses `borderLeft: 2px solid var(--err/warn)` directly. This keeps the run-list self-contained and avoids polluting the shared component vocabulary with run-list-specific styles.
- **D4 (search)** -- draft was unclear about whether a search input existed. It didn't. Added one to the header bar right side with magnify icon, clear button, and `/` keybinding with proper input/textarea/modal guards.

---

## Risks the next spec should know about

- **Component-level invariants preserved:** 5-state ReconcileChip untouched (run-detail.jsx not modified). PhaseMini unchanged.
- **New patterns introduced:** URL state helpers `_readUrlParams()` / `_writeUrlParams()` in run-list.jsx. Not exposed on `window` -- local to the file. `_matchesSearch()` for client-side text filtering. `_sortRuns()` for client-side column sorting.
- **Legacy StatusBadge still used in run-list** -- the inline-styled `StatusBadge` from shared.jsx is still consumed here. SPEC-0056/0057 can sweep it to the class-backed `SB` primitive when they touch the run-detail header.
- **Dead code still present:** PaneButton/PaneButtonGroup in run-detail.jsx, FilterChip in errors.jsx, ChromeTab in app.jsx. Carry-over from SPEC-0053/0054 handovers.

---

## Downstream invalidations (if any)

- **SPEC-0059 draft references D4 (`/`-bound search focus).** This is now shipped. SPEC-0059 can build on the existing `/` keybinding (e.g. extending to `j`/`k` row navigation) but doesn't need to add the basic `/` binding.
- Otherwise downstream drafts hold.

---

## Verification snapshot

- `uv run pytest tests/ -q` -> 725 passed.
- Preview-verify: dev server confirmed serving 13 runs with errored/deadlocked data. JSX serves correctly.
- Both themes: CSS uses token variables throughout; no hardcoded colors.
- Reduced-motion contract: no new animations added.
- Cache-bust: `?v=0055` in index.html (17 references).
- `/api/health`: `0.53.0` confirmed.

---

## For the orchestrator

- Next spec in queue: SPEC-0056.
- This spec's commit on main: `7f1bd13`.
- Branch deleted: local + remote.
