# Handover — SPEC-0070 (Run-detail header restructure)

**Date:** 2026-05-18
**Spec:** [0070](../specs/0070-run-detail-header.md)
**PR:** [#70](https://github.com/Lexiz/dual-research/pull/70)
**Version:** 0.62.0 -> 0.63.0
**Status:** `succeeded`
**Deploy:** `dual-research-alex.fly.dev/api/health` -> `{"ok":true,"version":"0.63.0","backend":"supabase"}`

---

## What shipped

Run-detail header restructured across three user-flagged areas. AgentStrip pills equalized via `flex: 1 1 0` in `.as` CSS so both agents share width equally regardless of model string length; vertical padding reduced from `var(--s-2)` to `4px` for a denser strip. The BlockingItemCallout banner ("N open . M ghosted . click to jump") removed entirely — same info available in the critique pane's DRIFT/OPEN section headers. Phase tab strip restructured with chip clusters: each tab now shows `P2 Negotiate 26 questions 10 disagreements` using full words instead of cramped abbreviations; zero counts render explicitly muted; active tab chips at full opacity, inactive muted.

### Changelog IDs implemented

SUR-05 (run-detail header restructure — partial revisit), SUR-PHASE-TAB-V2 (phase tab strip rebuild).

### Files touched

- `src/dual_research/ui/static/components.css` -- `.as` class: added `flex: 1 1 0`, reduced padding from `var(--s-2) var(--s-3)` to `4px var(--s-3)` (D1-D2).
- `src/dual_research/ui/static/run-detail.jsx` -- Removed `<BlockingItemCallout>` usage (D4). Restructured phase tab strip rendering with structured chip clusters: `P{N}` label + phase name + question/disagreement counts in full words (D6-D9).
- `src/dual_research/ui/static/design-language.jsx` -- AgentStrip spotlight updated: caption reflects equal-width + compact padding; showcase layout changed from vertical stack to horizontal flex (M1).
- `src/dual_research/ui/static/index.html` -- Cache-bust `?v=0069` -> `?v=0070` (22 references).
- `src/dual_research/ui/static/how-it-works.jsx` -- VERSION_NOTES entry for 0.63.0.
- `pyproject.toml` + `__init__.py` + `uv.lock` -- 0.62.0 -> 0.63.0.
- `CHANGELOG.md` -- 0.63.0 entry.
- `specs/0070-run-detail-header.md` -- spec doc.

### Mid-flight refinements

- **D1 (equalization)** -- Draft proposed `min-width: 320px` with `flex: 1 1 0` as alternatives. Shipped `flex: 1 1 0` in the `.as` CSS class directly (the `min-width: 320px` was already there from prior spec). This means ALL AgentStrip usages are equalized, not just the header. The modal agent strips also benefit — no per-call-site override needed.
- **D3 (internal layout)** -- Draft proposed changing the internal grid template. Kept the existing flex layout (`as-left` / `as-right`) since it already aligns correctly. No grid migration needed.
- **D5 (header row collapse)** -- Draft proposed collapsing from two rows to one after banner removal. The banner was actually BETWEEN the header and main content (not inside the header), so header row structure stays at two rows. Net vertical gain is still ~32px from the removed banner.
- **D6 (phase tab)** -- Draft proposed Chip primitives inside tabs. Shipped as styled spans with `mono` class instead — simpler, avoids nesting interactive elements, and achieves the same visual hierarchy.

---

## Risks the next spec should know about

- **AgentStrip `flex: 1 1 0` is now global** -- every AgentStrip usage will equalize width when siblings are in a flex container. This is intentional and correct for both header and modal contexts. SPEC-0071/0072 should verify modal agent strips still look right.
- **BlockingItemCallout component still exists in code** -- it's defined but no longer rendered (the usage line is commented out). Could be fully deleted in a future cleanup pass but harmless as dead code.
- **Phase tab counts show I/C counts via `phaseCountLabel` helper** -- the new structured rendering only shows questions + disagreements. Issues and comments counts are no longer visible in the tab label. This matches user intent (they only mentioned Q and D). If SPEC-0072 needs I/C counts, they can add them.
- **ReconcileChip 5-state preserved** -- untouched, verified on partner-vetting run.

---

## Downstream invalidations (if any)

None -- downstream drafts hold. SPEC-0071 (timeline structural) does not depend on header changes. SPEC-0072 (critique pane) should note that phase tab now shows structured chips, which affects the visual context of the critique pane header.

---

## Verification snapshot

- `uv run pytest tests/ -q` -> 744 passed (baseline preserved).
- Both themes: CSS uses token variables — no hardcoded colors introduced.
- Reduced-motion contract preserved: no new animations added.
- Cache-bust: `?v=0070` in index.html (22 references).
- `/api/health`: `0.63.0` confirmed.

---

## For the orchestrator

- Next spec in queue: SPEC-0071.
- This spec's commit on main: `0297611`.
- Branch deleted: local + remote.
