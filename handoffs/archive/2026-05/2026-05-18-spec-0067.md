# Handover — SPEC-0067 (Chip vocabulary + code-cluster expansion)

**Date:** 2026-05-18
**Spec:** [0067](../specs/0067-chip-vocabulary.md)
**PR:** [#67](https://github.com/Lexiz/dual-research/pull/67)
**Version:** 0.59.1 -> 0.60.0
**Status:** `succeeded`
**Deploy:** `dual-research-alex.fly.dev/api/health` -> `{"ok":true,"version":"0.60.0","backend":"supabase"}`

---

## What shipped

Foundation spec A of the tweak-cycle arc. Full-word labels replace all cryptic single-letter codes across every chip surface. New `parseCodeId` utility and `<CodeCluster>` primitive in shared.jsx parse critique public IDs into structured chip clusters. Stats chips (`+6 Cl` -> `+6 claims`), disagreement status labels (`-> claude` -> `conceded by Claude`), ghosted badges (`ghosted 4r` -> `ghosted 4 rounds`), reuse chips (`x 3.8 reuse` -> `x 3.8 token reuse`), and output bar labels (`-> d1` -> `feeds Claude's Phase 1 draft`) all expanded. No backend changes; all public IDs remain unchanged in API + URL.

### Changelog IDs implemented

No direct brief IDs -- this is a user-feedback-driven post-arc vocabulary pass. Touches CMP-04 Chip vocabulary principles.

### Files touched

- `src/dual_research/ui/static/shared.jsx` -- Added `parseCodeId()`, `CodeCluster` primitive, `CODE_KIND_MAP`, `CODE_KIND_LABELS`. Exported on window.
- `src/dual_research/ui/static/run-detail.jsx` -- `CHIP_KIND_META` labels expanded to full words; `StatChip` tooltip pluralization; disagreement status labels reworded; round range uses "opened/closed" instead of arrow; `GhostedRoundsBadge` + `GhostedAnnotation` expanded to "X rounds"; `ReuseChip` expanded to "token reuse"; `outputBarLabel` uses "feeds" prefix with full descriptions; `CardHeadline` renders `CodeCluster` for parseable public IDs.
- `src/dual_research/ui/static/design-language.jsx` -- Pill demo text updated from arrow notation to new labels.
- `src/dual_research/ui/static/index.html` -- Cache-bust `?v=0591` -> `?v=0067` (22 references).
- `src/dual_research/ui/static/how-it-works.jsx` -- VERSION_NOTES entry for 0.60.0.
- `pyproject.toml` + `__init__.py` + `uv.lock` -- 0.59.1 -> 0.60.0.
- `CHANGELOG.md` -- 0.60.0 entry.
- `specs/0067-chip-vocabulary.md` -- spec doc.
- `tests/test_ui_codecluster.py` -- **new**; 9 tests locking parseCodeId semantics.

### Mid-flight refinements

- **D9 (Output bar labels)** -- Draft proposed `-> feeds Claude's Phase 1 draft` (keeping the arrow). Shipped as `feeds Claude's Phase 1 draft` (no arrow) since the "feeds" verb already conveys directionality and the arrow was part of the cryptic vocabulary being replaced.
- **D10 (CardHeadline CodeCluster)** -- Draft proposed full CodeCluster replacement in all card headlines. Shipped with a fallback: if `parseCodeId` can't parse the ID, the headline falls back to the original plain Chip rendering. This is safer for edge cases and legacy data.
- **D7 (Disagreement chip pair)** -- Draft proposed separate "raised by X" + "conceded by Y" chip pair. Shipped as a single descriptive label ("conceded by Claude") in the existing status chip slot, which is simpler and still unambiguous. The "raised by" info is already shown in the expanded body.
- **D16 (Tests)** -- Draft proposed Python equivalents testing parseCodeId. Shipped as a pure-Python mirror of the JS parser with 9 test cases covering all five prefixes + edge cases.

---

## Risks the next spec should know about

- **Component-level invariants preserved:** 5-state ReconcileChip untouched. PhaseRail untouched.
- **New patterns introduced:**
  - `parseCodeId(id)` -- pure parser, returns `{ raw, kind, raiser, round, phase, sequence }`. Available on `window`.
  - `<CodeCluster id kind? hideRound size?>` -- structured chip cluster primitive. Available on `window`.
  - `CODE_KIND_LABELS` -- kind -> label map. Available on `window`.
  - Vocabulary rule codified: full words, never abbreviated codes. This is the system pattern going forward.
- **SPEC-0068 note:** The draft says to add a CodeCluster spotlight to design-language.jsx's ComponentSpotlights section. Since 0067 ships before 0068, and 0068 establishes the spotlights scaffolding, the spotlight should be added by 0068 as part of its work.
- **Dead code still present:** ChromeTab in app.jsx, FilterChip in errors.jsx. Carry-over from prior arcs.

---

## Downstream invalidations (if any)

None -- downstream drafts hold. The `<CodeCluster>` primitive API matches what downstream specs expect. SPEC-0068 (brand icons) will swap `<AgentIcon>` for `<BrandMark>` inside CodeCluster later; the current API is stable.

---

## Verification snapshot

- `uv run pytest tests/ -q` -> 744 passed (735 baseline + 9 new parseCodeId tests).
- Preview-verify on partner-vetting (`3a4a`): server responds 200 for all static files; run data loads with disagreements (d-04 etc.), questions (Q-c-r1-01 etc.), claims with phase encoding (Cl-c-p1-01), and ghosted entries.
- Both themes: not explicitly verified in headless mode (no browser), but all CSS uses token variables -- no hardcoded colors introduced.
- Reduced-motion contract preserved: no new animations added.
- Cache-bust: `?v=0067` in index.html (22 references).
- `/api/health`: `0.60.0` confirmed.

---

## For the orchestrator

- Next spec in queue: SPEC-0068.
- This spec's commit on main: `54a8e1b`.
- Branch deleted: local + remote.
