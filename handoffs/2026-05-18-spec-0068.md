# Handover — SPEC-0068 (Brand-icon system + Design-page DNA reskin)

**Date:** 2026-05-18
**Spec:** [0068](../specs/0068-brand-icon-system.md)
**PR:** [#68](https://github.com/Lexiz/dual-research/pull/68)
**Version:** 0.60.0 -> 0.61.0
**Status:** `succeeded`
**Deploy:** `dual-research-alex.fly.dev/api/health` -> `{"ok":true,"version":"0.61.0","backend":"supabase"}`

---

## What shipped

Foundation spec B of the tweak-cycle arc. Two coupled workstreams: (1) a new `<BrandMark>` primitive with `BRAND_SVGS` single-source dictionary, migrating all agent-identifying surfaces to use official brand glyphs, and (2) a Design Language page restructure splitting into a curated DNA one-pager (default) and the full reference (at `?full=1`). The DNA page becomes the live source-of-truth for the design system from this spec onward, with component spotlight scaffolding for future specs.

### Changelog IDs implemented

CMP-NEW (BrandMark primitive), SUR-NEW-DESIGN-PAGE (DNA one-pager). Indirectly addresses brief S11 brand fidelity.

### Files touched

- `src/dual_research/ui/static/shared.jsx` -- Added `BRAND_SVGS` dict, `BrandMark` primitive (exposed on window). `AgentIcon` refactored to delegate to BRAND_SVGS. CodeCluster agent chip now uses `<BrandMark>` inline glyph. MonoGram functions reference BRAND_SVGS.
- `src/dual_research/ui/static/design-language.jsx` -- Restructured: `DesignLanguageView` routes between `DnaOnePager` (default) and `FullReference` (`?full=1`). New DNA sections: Hero, Palette (compact swatch row), Brand marks (using `<BrandMark>`), Component spotlights (Chip/Card/Tab/AgentStrip with M1 scaffolding), Construction (three principles). Full reference preserves all original content. `BrandCard` / `BrandSwatch` updated to use `<BrandMark>` instead of `AgentIcon`.
- `src/dual_research/ui/static/run-list.jsx` -- Dual-color gradient square replaced with two 9px `<BrandMark>` glyphs (Claude top-left, OpenAI bottom-right) on bg-3 background.
- `src/dual_research/ui/static/index.html` -- Cache-bust `?v=0067` -> `?v=0068` (22 references).
- `src/dual_research/ui/static/how-it-works.jsx` -- VERSION_NOTES entry for 0.61.0.
- `pyproject.toml` + `__init__.py` + `uv.lock` -- 0.60.0 -> 0.61.0.
- `CHANGELOG.md` -- 0.61.0 entry.
- `specs/0068-brand-icon-system.md` -- spec doc.

### Mid-flight refinements

- **D6 (Run-list composition)** -- Draft proposed two 8x8 BrandMark halves or a single 16x16 duo SVG. Shipped as two 9px BrandMarks positioned absolutely (Claude top-left, OpenAI bottom-right) in a 16x16 container with bg-3 background. This reads as "app brand" rather than "two icons crammed in a square."
- **D7/D8 (Critique-card + modal attribution chips)** -- Draft proposed per-site BrandMark prefixes on all attribution chips. Shipped by letting AgentIcon (which all those sites already use) delegate to BRAND_SVGS for the path data, plus switching CodeCluster's agent chip specifically to the bare BrandMark glyph. No per-call-site edits needed for the tiled AgentIcon usages since they already get the brand SVG automatically.
- **D10 (Design page BrandMarksSection)** -- Draft proposed stripping inline SVG duplicates. Shipped by having BrandCard/BrandSwatch call `<BrandMark>` directly instead of `AgentIcon`, consuming the same BRAND_SVGS dict.

---

## Risks the next spec should know about

- **Component-level invariants preserved:** 5-state ReconcileChip untouched. PhaseRail untouched. AgentDuoVisual untouched.
- **New patterns introduced:**
  - `BRAND_SVGS` dict -- `{ claude: <path>, openai: <path> }`. Single source of truth for brand glyphs.
  - `<BrandMark name="claude"|"openai" size variant="solid"|"ghost">` -- standalone SVG glyph. Exposed on `window`.
  - `DnaOnePager` / `FullReference` routing in design-language.jsx via `?full=1` URL param.
  - `<Spotlight label caption>` component for the component showcase section.
  - M1 scaffolding comment block in ComponentSpotlights: `{/* M1: subsequent specs add their new primitives here. */}`
- **Design page is now the live SoT.** Subsequent specs adding new primitives should add a `<Spotlight>` entry in the marked block.
- **AgentIcon API unchanged.** All existing call sites work; the only visual change is that the SVG path now comes from BRAND_SVGS.

---

## Downstream invalidations (if any)

None -- downstream drafts hold. The `<BrandMark>` primitive API matches what downstream specs expect. The DNA page spotlight scaffolding is ready for SPEC-0071 (`<CollapsibleSection>`) and SPEC-0073 (`<QuoteCallout>`) to add their entries.

---

## Verification snapshot

- `uv run pytest tests/ -q` -> 744 passed (no new Python tests; same 744 as 0067 baseline).
- Preview-verify on partner-vetting: server responds 200 for canonical fixture; health returns v0.61.0.
- Both themes: all CSS uses token variables -- no hardcoded colors introduced.
- Reduced-motion contract preserved: no new animations added.
- Cache-bust: `?v=0068` in index.html (22 references).
- `/api/health`: `0.61.0` confirmed.

---

## For the orchestrator

- Next spec in queue: SPEC-0069.
- This spec's commit on main: `0718b9f`.
- Branch deleted: local + remote.
