# Handover -- SPEC-0053 (Tab system (3 variants) + table header)

**Date:** 2026-05-17
**Spec:** [0053](../specs/0053-tab-system.md)
**PR:** [#57](https://github.com/Lexiz/dual-research/pull/57)
**Version:** 0.50.0 -> 0.51.0
**Status:** `succeeded`
**Deploy:** `dual-research-alex.fly.dev/api/health` -> `{"ok":true,"version":"0.51.0","backend":"supabase"}`

---

## What shipped

Unified Tab primitive with three CSS variants (default bordered pill, tabs-line underline stripe, tabs-solid segmented control) and matching React `Tab` + `TabGroup` components in shared.jsx. Four legacy inline-styled components migrated to the new primitive: PaneButton call sites in run-detail.jsx (critique pane phase buttons, filter chips, conversation/consumption toggle), FilterChip in errors.jsx, run-list filter strip, and ChromeTab in app.jsx. Table header distinction (CMP-11) added to both the generic `.tbl` primitive and the bespoke run-list grid header. Dead `PhaseTab` function removed.

### Changelog IDs implemented

`CMP-01` (Tab active state -- dropped 2px info underline, replaced with fill + semibold), `CMP-08` (Tab 3 variants), `CMP-11` (table header distinction).

### Files touched

- `src/dual_research/ui/static/components.css` -- added ~186 lines: Tab, tab-group, tabs-line, tabs-solid, tab-filter variants + .tbl table styling
- `src/dual_research/ui/static/shared.jsx` -- added Tab + TabGroup React primitives, exposed on window
- `src/dual_research/ui/static/run-detail.jsx` -- migrated PaneButton call sites to Tab/TabGroup (TimelineTabs, CritiqueExplorer header, CritiqueTypeFilter); removed dead PhaseTab function
- `src/dual_research/ui/static/run-list.jsx` -- migrated filter strip to Tab; upgraded grid header (CMP-11)
- `src/dual_research/ui/static/errors.jsx` -- migrated FilterChip call sites to Tab
- `src/dual_research/ui/static/app.jsx` -- migrated ChromeTab to Tab
- `src/dual_research/ui/static/index.html` -- cache-bust ?v=0053
- `src/dual_research/ui/static/how-it-works.jsx` -- VERSION_NOTES entry for 0.51.0
- `pyproject.toml` + `__init__.py` -- 0.50.0 -> 0.51.0
- `CHANGELOG.md` -- 0.51.0 entry
- `specs/0053-tab-system.md` -- spec doc
- `uv.lock` -- version sync

### Mid-flight refinements

- **D7 (ViewSwitcher)** -- the draft expected a `ViewSwitcher` component in app.jsx. Actual state of main has `ChromeTab` (a simpler inline-styled button). Migrated `ChromeTab` usage to `<Tab>` with MDI icon prop instead.
- **D9 (PhaseTab dead code)** -- the draft expected PhaseTab was still in active use. It was dead code (never called since spec 0046 migrated phase tabs to PaneButton). Removed the definition.
- **D5 (PhaseTab migration)** -- not applicable since PhaseTab was dead code. Phase tabs in the critique pane were already using PaneButton, which we migrated to Tab via D4.
- **D10 (legacy definitions)** -- PaneButton and PaneButtonGroup definitions left in place as planned; their definitions are in run-detail.jsx and will be removed when a future spec next touches that file.

---

## Risks the next spec should know about

- **PaneButton/PaneButtonGroup still defined** in run-detail.jsx (lines ~108-172). Not called from any migrated site, but the definitions remain. SPEC-0056 or SPEC-0057 should remove them when they next touch run-detail.jsx.
- **FilterChip still defined** in errors.jsx (line ~248). Call sites migrated but definition left. Remove when errors.jsx is next touched.
- **ChromeTab still defined** in app.jsx (line ~144). Call sites migrated but definition left. Remove when app.jsx is next touched.
- **tabs-line variant** has no consumers yet. SPEC-0058 (Modal + sub-tabs) will be its first user.
- **Component-level invariants preserved:** 5-state ReconcileChip untouched.

---

## Downstream invalidations (if any)

- **SPEC-0054 draft assumes PhaseTab exists.** It doesn't -- removed in this spec. If 0054 planned to touch PhaseTab, it's moot.
- **SPEC-0055 draft expects run-list filter strip to be inline-styled buttons.** They're now `<Tab>` primitives. The filter logic and data flow are identical; only the rendering changed.
- **SPEC-0056 draft expects PaneButton in run-detail.jsx.** PaneButton call sites are now Tab/TabGroup; PaneButton definition still exists but is dead. 0056 can safely remove the definition.

---

## Verification snapshot

- `uv run pytest tests/ -q` -> 725 passed.
- Preview-verify on partner-vetting (`3a4a`): confirmed via API (runs load, ReconcileChip 5-state intact).
- Both themes: CSS includes `body.light .tab-group.tabs-solid .tab.is-active` override.
- Reduced-motion contract preserved: Tab transitions use `var(--m-fast)` / `var(--m-base)` tokens.
- Cache-bust: `?v=0053` in index.html.
- `/api/health`: `0.51.0` confirmed.

---

## For the orchestrator

- Next spec in queue: SPEC-0054.
- This spec's commit on main: `502cfb5`.
- Branch deleted: local + remote.
