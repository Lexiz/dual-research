---
kind: draft
draft_id: "004"
slug: critique-refresh
title: Critique pane refresh — align live pane with design-system reference
type: unclassified
status: draft
created: 2026-05-22
source_session: pre-lifecycle-bootstrap
parked_from: prototypes/critique-iteration/ (untracked working files)
---

# Draft 004 — Critique pane refresh

> **Status:** unclassified. Same parking pattern as draft-003 (timeline refresh).

---

## What this is

Pre-spec iteration captured in [`prototypes/critique-iteration/`](../../prototypes/critique-iteration/). Same harness pattern as the timeline iteration — a workshop mockup with Iteration / Live / DS tabs, phase navigation, light + dark variants. Captures the design-system target for the critique pane (which the live UI currently diverges from in subtle ways).

The original kickoff prompt for this iteration is at `prototypes/critique-iteration/KICKOFF-PROMPT.md` (legacy `_kickoff-prompts/` style, superseded by the lifecycle system but preserved here as historical context for whoever picks this up).

## Unresolved questions

- Is this one spec or several? (Phase 0 / Phase 2 / Phase 4 / Σ Summary cards may have different fixes.)
- Type — `refactoring` (token-level alignment with DS) or `new-feature` (visible UX change)? Depends on scope.
- Does this overlap with draft-001 (Summary tab v2) or 002 (Login screen v2) — both touch UI surfaces. If yes, sequence matters.

## Next step

Either:

1. Iterate in a fresh authoring session to flesh out scope and call `/spec-promote 004` when complete.
2. Or `rm -rf prototypes/critique-iteration/ specs/drafts/draft-004-critique-refresh.md` to discard.
