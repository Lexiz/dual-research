# Kickoff prompt — Spec 0140

> **Title:** Phase 4 deadlock — draft extractor body retention + escape-valve breadth for terminal-ledger AGREED
> **Target version:** v1.9.1 (PATCH)
> **Source spec:** [specs/0140-phase4-deadlock-extractor-and-escape-valve.md](../0140-phase4-deadlock-extractor-and-escape-valve.md)
> **Depends on (must be merged on main):** 0137 (already on main as of v1.8.3)
> **Inventory rows covered:** B07, B12, B13

---

## Kickoff — paste into new session

You're picking up **Spec 0140 — Phase 4 deadlock: draft extractor body retention + escape-valve breadth** in the dual-research project.

This is a **blocker bug fix**. Phase 4 deadlocks at turn 8 on the anchor run because the draft extractor drops body text under `##` sub-sections (B12), which produces empty turns that prevent Spec 0137's substantive-convergence escape valve from firing (B13), which leaves the run stuck in a hard-cap loop (B07). One causal chain, one PATCH spec.

### 1. Open the repo and validate state

```bash
cd /Users/alexlisitzky/dual-research
git checkout main
git pull origin main
git status   # expect: clean tree on main, up to date with origin
```

Read these in order before touching anything:

1. The most recent file in `handoffs/` — context on what was merged last
2. `specs/0140-phase4-deadlock-extractor-and-escape-valve.md` end-to-end
3. The spec's blockquote `Depends on` — confirm Spec 0137 is on main (it was merged as v1.8.3)
4. `specs/_backlog-inventory.md` rows B07 / B12 / B13 — source descriptions

### 2. Re-validate spec against current main

The spec was drafted 2026-05-21 against main at `c882e17`. Before implementing:

- Walk every `src/dual_research/.../file.py:LINE` citation in the spec. If lines have drifted, propose the spec edit to the user before coding.
- Re-confirm the anchor-run evidence (`seq 270` hard-cap exit, `draft-v7.md` 76-byte stub) by re-querying Supabase if uncertain. Run id: `20260521-010637-dvs-backend-language-choice`.

### 3. Open questions

**None.** The fix surfaces, the precondition widening shape, and the gating round all derive directly from the anchor run's event stream and on-disk artifacts. Proceed straight to implementation.

### 4. Implement

```bash
git checkout -b spec-0140-phase4-deadlock-extractor-and-escape-valve
```

Follow §5 (Proposed change) literally:

- Switch `dr_run.py` to the inclusive draft extractor
- Add the Spec 0114 protocol sentinels (`## Stance`, `## Addressing…`, `## Ratifying…`, `## New items…`, `## Phase artifact`, `## Status`, `## Closeout constraints`) to `_PROTOCOL_TOP_HEADINGS`
- Widen Spec 0137's escape-valve precondition to fire on one-agent-AGREED + terminal ledger past the soft cap

Respect §6 (Out of scope) — no prompt rewrite, no closeout changes.

### 5. Verify

Run every checkbox in §7 (Test plan):
- Unit tests for the inclusive extractor on `##` sub-sections
- Unit tests for the widened escape-valve precondition
- Replay test against anchor run `20260521-010637-dvs-backend-language-choice` — confirm it now exits with `via_artifact_promotion=True` at the natural convergence round instead of hard-cap

### 6. Ship

```bash
git push -u origin spec-0140-phase4-deadlock-extractor-and-escape-valve
gh pr create --title "Spec 0140 — Phase 4 deadlock extractor + escape-valve breadth (v1.9.1)" --body "$(cat <<'EOF'
## Summary
- Inclusive draft extractor retains `##` sub-sections (B12)
- Spec 0137 escape valve widens to one-agent-AGREED + terminal ledger past soft cap (B13)
- Anchor-run replay terminates at natural convergence instead of hard-cap deadlock (B07)

## Test plan
- [x] Unit: extractor retains `##` sub-section bodies
- [x] Unit: widened escape-valve precondition fires on terminal ledger + one-AGREED
- [x] Replay: anchor run 20260521-010637 exits via_artifact_promotion=True

## Anchor run
20260521-010637-dvs-backend-language-choice

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
gh pr merge --squash --delete-branch
git checkout main && git pull
```

### 7. Deploy

```bash
fly deploy
```

Wait healthy. Smoke-check by firing a quick run via `/dual-research-run` on a brief known to drift in Phase 4; confirm it converges at natural agreement.

### 8. Write the handover

Create `handoffs/2026-05-21-spec-0140-phase4-deadlock-extractor-and-escape-valve.md` on main with: what landed, files touched, deploy status, version (v1.9.1), known follow-ups (if any). Commit + push.

### 9. Stop

Per the pause-between-specs rule, **do not start Spec 0141** in this session. Confirm main is clean and the handover is up, then exit.

---

## Cross-reference

- Source spec: [specs/0140-phase4-deadlock-extractor-and-escape-valve.md](../0140-phase4-deadlock-extractor-and-escape-valve.md)
- Inventory rows: B07, B12, B13 in [specs/_backlog-inventory.md](../_backlog-inventory.md)
- Anchor run: `20260521-010637-dvs-backend-language-choice`
- Batch commit: `c882e17`
