# Kickoff prompt — Spec 0144

> **Title:** Sources & provenance — investigation outcome and per-critique-card surface (incl. Phase 4 Issue/Comment patches)
> **Target version:** v1.10.0 (MINOR)
> **Source spec:** [specs/0144-sources-provenance-investigation-and-critique-card-surface.md](../0144-sources-provenance-investigation-and-critique-card-surface.md)
> **Depends on (must be merged on main):** 0140, 0141
> **Inventory rows covered:** B09, B14, B08

---

## Kickoff — paste into new session

You're picking up **Spec 0144 — Sources & provenance: investigation outcome + critique-card surface**.

This spec carries B09's investigation outcome (provenance fires partially — 19 turn_searches events and 14 of 78 transitions carry evidence_records on the anchor run; the validator is wired to a default no-op; the address-side drops flagged blocks instead of annotating them; `_normalizeToThread` in run-detail.jsx:6419 strips `evidence` before it reaches `QuestionThread`; `ItemCard` exists with full source rendering but has zero call-sites). Plus B14 (the feature surface) and B08 (Phase 4 Issue/Comment patches — absorbed via the same `ItemCard` render switch).

Three workstreams (backend + frontend + design-system) in one spec.

### 1. Open the repo and validate state

```bash
cd /Users/alexlisitzky/dual-research
git checkout main
git pull origin main
git status
```

Confirm 0140 **and** 0141 are merged (`git log --oneline -15`). If either is missing, **stop and tell the user**.

Read:
1. Latest `handoffs/` file (will likely be 0141's; review whether B10's contingency triggered a followup that affects this spec)
2. `specs/0144-sources-provenance-investigation-and-critique-card-surface.md` end-to-end
3. `specs/_backlog-inventory.md` rows B09 / B14 / B08
4. Skim `specs/0042-critique-data-integrity.md` and `specs/0119-badge-governance.md`

### 2. Re-validate spec against current main

Drafted 2026-05-21 against `c882e17`. 0141 may have changed item-state code; reconfirm the §5 (Current-state audit) tables, especially the `_normalizeToThread` line in `run-detail.jsx` and the `apply_turn` paths.

Re-confirm the anchor-run evidence-record stats (14 of 78 transitions carrying `evidence_records`) by re-querying Supabase. Run id: `20260521-010637-dvs-backend-language-choice`.

### 3. Open questions

Three open questions:

1. **`search_N` ↔ `event_id` mapping across providers.** Anthropic returns opaque `srvtoolu_…`; OpenAI may be sequential. The §9.5 closure assumes same ordering; verify by inspecting an OpenAI-side turn before merging. If asymmetric, add a provider-specific normaliser.
2. **`Sources N` header chip click affordance — per-card-jump or per-row-jump?** Default: per-card-jump (consistent with timeline category chips). Confirm with user.
3. **Card-level vs row-level "unverified" chip?** Spec leans card-level for noise control; confirm with user before implementing the visual.

### 4. Implement

```bash
git checkout -b spec-0144-sources-provenance-investigation-and-critique-card-surface
```

§6 has three sub-sections — implement in order:
- 6.1 Backend: turn on the provenance validator, retain Issue/Comment patches in the address path
- 6.2 Frontend: stop stripping `evidence` in `_normalizeToThread`, route through `ItemCard`
- 6.3 Design-system: any new source-chip primitives + tokens

Respect §7 (Out of scope) — no protocol rewrite, no sources page, no cross-run dashboards.

### 5. Verify

§8 checklist: investigation-closure verification (provenance fires on every turn that emits a search); patch retention tests; visual regression for the new card surface; anchor-run replay.

### 6. Ship

```bash
git push -u origin spec-0144-sources-provenance-investigation-and-critique-card-surface
gh pr create --title "Spec 0144 — Sources & provenance: investigation + critique-card surface (v1.10.0)" --body "$(cat <<'EOF'
## Summary
- B09 outcome: provenance was firing partially (14/78 transitions); validator default no-op + address-side drop + frontend strip — all closed
- B14 feature: per-card Sources chip + visible provenance on every critique card
- B08 absorbed: Phase 4 Issue/Comment patches retained via same ItemCard render path

## Test plan
- [x] Validator fires on every search-bearing turn
- [x] Patch retention for Issue/Comment fragments
- [x] Visual regression: critique cards with sources render the new surface
- [x] Anchor-run replay shows 100% provenance coverage on search-bearing turns

## Anchor run
20260521-010637-dvs-backend-language-choice

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
gh pr merge --squash --delete-branch
git checkout main && git pull
```

### 7. Deploy

```bash
fly deploy
```

Smoke: open the anchor run, confirm critique cards now show a Sources chip. Open a Phase 4 turn modal, confirm Issue and Comment patches render.

### 8. Write the handover

`handoffs/2026-MM-DD-spec-0144-sources-provenance-investigation-and-critique-card-surface.md`. Include: investigation outcomes (what changed in the validator wiring), three OQ resolutions, any design-system tokens added, backfill plan for historic runs.

### 9. Stop

Do not start Spec 0145. Exit.

---

## Cross-reference

- Source spec: [specs/0144-sources-provenance-investigation-and-critique-card-surface.md](../0144-sources-provenance-investigation-and-critique-card-surface.md)
- Inventory rows: B09, B14, B08 in [specs/_backlog-inventory.md](../_backlog-inventory.md)
- Anchor run: `20260521-010637-dvs-backend-language-choice`
- Batch commit: `c882e17`
